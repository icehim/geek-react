/*
* 创建路由的history对象=>提供页面跳转方法
* */
//createBrowserHistory(history模式) | createHashHistory(hast模式)
import {createBrowserHistory} from "history";

const customHistory = createBrowserHistory(

)
export default customHistory
